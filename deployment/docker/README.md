# Blueprint Implementation Using Docker Compose

### How to run:
```shell
cd deployment/docker
```

```shell
docker compose up
```
Prerequisites:
* Required docker version: Docker version 24.0.2 (At least)
